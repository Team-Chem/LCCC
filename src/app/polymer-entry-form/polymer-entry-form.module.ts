// export class PolymerEntry{
//   polymerName: string;
//   molarMassRange: [number, number]; //This syntax is for a tuple.
//   solvents: string[];
//   composition: string;  // Add composition here if it's necessary
//   diameter: number;
//   poreSize: number;
//   columnLength: number;
//   columnName: string;
//   temperature: number;
//   pressure: number;
//   flowRate: number;
//   injectionVolume: number;
//   detectors: string[];
//   DOI: string;
//
//   constructor(
//     polymerName: string, molarMassRange: [number, number], solvents: [string, string], composition: string,
//     diameter: number, poreSize: number, columnLength: number, columnName: string, temperature: number,
//     pressure: number, flowRate: number, injectionVolume: number, detectors: string[], DOI: string){
//
//     //Just setting all the defaults to be empty.
//     this.polymerName = polymerName;
//     this.molarMassRange = molarMassRange;
//     this.solvents = solvents;
//     this.composition = composition;  // And finally in the assignment
//     this.diameter = diameter;
//     this.poreSize = poreSize;
//     this.columnLength = columnLength;
//     this.columnName = columnName;
//     this.temperature = temperature;
//     this.pressure = pressure;
//     this.flowRate = flowRate;
//     this.injectionVolume = injectionVolume;
//     this.detectors = detectors;
//     this.DOI = DOI;
//   }
// }
